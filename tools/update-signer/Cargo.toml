[package]
name = "update-signer"
version = "0.1.0"
edition = "2021"
description = "希尔娅 update.json / update-apk.json 签名工具（CI 用）"

[dependencies]
base64 = "0.22"
ed25519-dalek = "2.1"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[profile.release]
strip = true
